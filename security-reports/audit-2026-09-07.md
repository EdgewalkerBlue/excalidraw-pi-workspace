# npm 依赖漏洞检查报告 — 2026-09-07

- 项目：`excalidraw-workspace@0.1.0`（D:\projects\excalidraw-workspace）
- 工具：`npm audit`（npm 10.9.8），审计范围 559 个依赖（prod 420 / dev 138 / optional 85）
- 性质：只读扫描 + `--dry-run` 修复预览，**未改动任何项目文件**

## 一、漏洞概览

| 严重程度 | 数量 |
| --- | --- |
| critical | 0 |
| high | 0 |
| **moderate** | **7** |
| low | 0 |

7 个 moderate 漏洞全部集中在直接依赖 `mcp-excalidraw-server@2.0.0`（Node 服务端，绑定 :5001）的子依赖中，涉及 3 个根因包：**mermaid、qs、uuid**。前端 bundle 中的 `@excalidraw/excalidraw@0.18.1 → mermaid-to-excalidraw@2.2.2 → mermaid@11.17.2` 已是修复版本，**不受影响**。

## 二、漏洞明细

### 1. mermaid@10.9.4（7 条告警中的 6 条独立公告）

- 安装路径：`node_modules/mcp-excalidraw-server/node_modules/@excalidraw/mermaid-to-excalidraw/node_modules/mermaid`
- 依赖链：`mcp-excalidraw-server@2.0.0 → @excalidraw/mermaid-to-excalidraw@1.1.4 → mermaid@10.9.4`
- 受影响范围：`<=10.9.6 || 11.0.0-alpha.1 - 11.4.1`；修复版本：**10.9.8**（10.x 内补丁）或 ≥11.4.2

| 公告 | 类型 | CVSS |
| --- | --- | --- |
| [GHSA-ghcm-xqfw-q4vr](https://github.com/advisories/GHSA-ghcm-xqfw-q4vr) | 状态图 classDef 未消毒 → HTML 注入（CWE-94） | - |
| [GHSA-xcj9-5m2h-648r](https://github.com/advisories/GHSA-xcj9-5m2h-648r) | classDefs 未消毒 → CSS 注入（CWE-94） | - |
| [GHSA-6m6c-36f7-fhxh](https://github.com/advisories/GHSA-6m6c-36f7-fhxh) | 甘特图死循环 DoS（CWE-835） | 5.3 |
| [GHSA-87f9-hvmw-gh4p](https://github.com/advisories/GHSA-87f9-hvmw-gh4p) | 配置未消毒 → CSS 注入（CWE-94） | - |
| [GHSA-c4c3-pg64-4m4v](https://github.com/advisories/GHSA-c4c3-pg64-4m4v) | 配置 API 原型污染（CWE-1321，low） | - |
| [GHSA-6x64-9x62-f2gx](https://github.com/advisories/GHSA-6x64-9x62-f2gx) | CSS 注入波及相邻元素（CWE-94） | - |
| [GHSA-2v8p-3f2j-5mp7](https://github.com/advisories/GHSA-2v8p-3f2j-5mp7) | XY 图死循环 DoS（CWE-835/1325） | - |

### 2. qs@6.15.3

- 安装路径：`node_modules/qs`（被 express 直接依赖，body-parser 传递依赖）
- 依赖链：`mcp-excalidraw-server@2.0.0 → express@4.22.2 → body-parser@1.20.6 → qs@6.15.3`（express 也直接依赖 qs@6.15.3）
- 受影响范围：`2.2.5 - 6.15.3`；修复版本：**6.16.0**（6.x 内 semver 兼容）

| 公告 | 类型 | CVSS |
| --- | --- | --- |
| [GHSA-x5fp-wj9c-mxmx](https://github.com/advisories/GHSA-x5fp-wj9c-mxmx) | bracket-key 逗号解析绕过 array-limit → 资源耗尽（CWE-770） | 3.7 |
| [GHSA-4mjr-xmp4-gh2g](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g) | 攻击者可控 isBuffer → DoS（CWE-248/703） | 5.3 |

### 3. uuid@9.0.1

- 安装路径：`node_modules/mcp-excalidraw-server/node_modules/uuid`
- 依赖链：`mcp-excalidraw-server@2.0.0 → @excalidraw/mermaid-to-excalidraw@1.1.4 → mermaid@10.9.4 → uuid@9.0.1`
- 受影响范围：`<11.1.1`；修复版本：**11.1.1+**（跨大版本）
- [GHSA-w5hq-g745-h8pq](https://github.com/advisories/GHSA-w5hq-g745-h8pq)：v3/v5/v6 在显式传入 `buf` 时缺缓冲区边界检查（CVSS **7.5**，本次扫描中 CVSS 最高的一条）。注意：漏洞触发需要调用方显式传 `buf` 参数，mermaid 仅以无参 `v4()` 方式使用 uuid，**在本项目实际不可利用**。

### 4. 连带标记（无独立公告，随根因包消除）

- `mcp-excalidraw-server@2.0.0`（直接依赖）、`@excalidraw/mermaid-to-excalidraw@1.1.4`、`express@4.22.2`、`body-parser@1.20.6`：均为「依赖了漏洞包」的间接标记。

## 三、修复预览（npm audit fix --dry-run）

- `npm audit fix`（不带 `--force`）：预览结果 `changed: 0` —— **没有任何 semver 兼容的自动修复可用**（输出中 added 71 个包为 esbuild/rollup/lightningcss 等可选平台二进制的常规重整，非修复动作）。
- `npm audit fix --force`：npm 给出的唯一自动路径是 **把 `mcp-excalidraw-server` 从 2.0.0 降级到 1.0.5**（semver-major 破坏性降级）。npm registry 上该包最新即 2.0.0，上游暂无包含修复依赖的 v2 补丁版本。

## 四、建议修复方案（供决策，未执行）

项目已有 `overrides` 钉扎先例（lodash-es / dompurify / nanoid），建议沿用该方式逐包钉扎，而不是接受 `--force` 降级：

1. **qs（优先，低风险）**：在 `package.json` 的 `overrides` 加 `"qs": "^6.16.0"`（6.x 内 minor 升级，API 不变）。消除 qs 的 2 条公告，并连带清除 express / body-parser 的间接标记。
2. **mermaid（次优先，低风险）**：加 `"@excalidraw/mermaid-to-excalidraw@1.1.4": { "mermaid": "10.9.8" }` 这类**限定父包版本**的 scoped override，只把服务端子树里的 mermaid@10.9.4 升到 10.9.8 补丁版；避免全局钉扎把前端的 mermaid@11.17.2 降级。消除 6 条公告（4 条注入 + 2 条 DoS + 1 条原型污染）。改完用 `npm ls mermaid` 验证前端仍为 11.x。
3. **uuid（可接受现状）**：该公告（CVSS 7.5）需调用方显式传 `buf` 才可触发，mermaid 只用无参 `v4()`，本项目实际不可利用。可暂不处理、跟随上游更新；若要消除告警，需跨大版本钉扎 `"uuid": "^11.1.1"`，改动 mermaid@10.x 的依赖，需回归测试后再加。
4. **不建议** `npm audit fix --force`：会将 mcp-excalidraw-server 2.0.0 降级到 1.0.5（破坏性），丢失 v2 能力（近期提交 `patch-server v3 适配` 即基于 v2）。
5. **中风险项跟踪**：qs 的 DoS 公告面向网络攻击者，而项目 SECURITY.md 描述服务端以 `0.0.0.0:5001` 无认证绑定局域网 —— 该告警的实际暴露面高于一般本地服务，建议优先落地第 1 项。

## 五、现有 overrides 与本次告警的关系

`package.json` 现有钉扎：`lodash-es 4.18.1`、`dompurify ^3.4.14`、`nanoid 5.1.16`。本次 7 条告警的根因包（mermaid / qs / uuid）均不在其覆盖范围内，**无一条被现有钉扎掩盖或影响**，全部为真实存在的待处理漏洞。

## 六、修复记录（2026-09-08）

已按第四节方案实施修复，全程使用 `overrides` 钉扎，未使用 `npm audit fix --force`：

- `package.json` overrides 新增两项：
  - `"qs": "^6.16.0"`（6.x 内 minor 升级，API 不变）→ 消除 qs 的 2 条公告，连带清除 express / body-parser 的间接标记。
  - `"mcp-excalidraw-server": { "@excalidraw/mermaid-to-excalidraw": "^2.2.2" }`（嵌套钉扎，仅作用于 mcp-excalidraw-server 子树）→ 服务端 mermaid-to-excalidraw 1.1.4 升至 2.2.2，其依赖 mermaid `^11.12.1` 与前端 dedupe 为 11.17.2，消除 mermaid 全部 6 条公告；uuid@9.0.1 随之从依赖树消失，GHSA-w5hq-g745-h8pq 一并解决（无需跨大版本强钉 uuid）。
- **实施要点**：npm overrides 不支持「父包名@版本」限定写法——`"@excalidraw/mermaid-to-excalidraw@1.1.4": {...}` 这种键会被 npm 当作不限版本的包名，误伤前端同包 2.2.2 实例（曾导致前端 mermaid 被降到 10.9.8 的 invalid 状态，已回退并清理残留）。m-t-e 升 2.2.2 对服务端运行时零风险：`parseMermaidToExcalidraw` 仅出现在其预构建前端 bundle（`dist/frontend/assets/*`，自包含产物），Node 运行时代码不导入该包。
- **验证结果**：
  - `npm audit`：**found 0 vulnerabilities**（修复前 7 moderate）
  - `npm ls mermaid uuid qs @excalidraw/mermaid-to-excalidraw`：依赖树干净、无 invalid；前端 mermaid@11.17.2 / uuid@14.0.2 不变，qs 全树 6.16.0
  - `npm test`：4/4 通过
  - `npm run build`（tsc -b && vite build）：成功（仅原有的 chunk 体积提示）
- 改动文件：`package.json`（overrides +2 项）、`package-lock.json`。

---
*报告由定时任务自动生成（一次性，今晚 23:00 触发）；扫描与修复过程均未使用破坏性 `--force` 操作。*
